# Central place to declare which fine-tuned model to use in scripts.
# Copy this file to scripts/models.env and adjust values.

# Preferred: explicit fine-tuned model identifier or local path
# Examples:
# - HuggingFace Hub model id (private org ok if auth is configured)
#   FINETUNED_MODEL_NAME=your-org/bert-base-uncased-ft-sst2
# - Local directory containing a HF-compatible checkpoint (config + tokenizer)
#   FINETUNED_MODEL_NAME=./outputs/llm/stanfordnlp/sst2_2025-06-01_12-00-00

# FINETUNED_MODEL_NAME=

# Project-wide default model name (used if FINETUNED_MODEL_NAME is unset)
# GRAPHTEXT_MODEL_NAME=your-org/bert-base-uncased-ft-agnews

# Optional global path to a finetuned weights file (.pt) to be loaded into the base model
# GRAPHTEXT_WEIGHTS_PATH=/app/outputs/finetuned_llms/setfit/ag_news/model_epoch_4.pt

# If you serve models via a custom HF mirror, you can also set:
# HF_ENDPOINT=https://hf-gateway.internal.example.com
# TRANSFORMERS_CACHE=/path/to/shared/cache
